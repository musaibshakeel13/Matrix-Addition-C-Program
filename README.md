# Matrix-Addition-C-Program
This repository contains a simple C program that performs element-wise addition of two 3×3 matrices using 2D arrays. The program stores values in two arrays, adds them using nested loops, and prints the resulting matrix.
